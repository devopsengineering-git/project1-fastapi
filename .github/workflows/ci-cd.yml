name: CI/CD Pipeline for FastAPI Project

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: ⬇️ Checkout code
      uses: actions/checkout@v3

    - name: 🐍 Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: 📦 Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: ✅ Run tests
      run: |
        echo "No tests added yet — you can integrate pytest or unittest here"

    # Optional: Docker build step — only if needed now or later
    # - name: 🐳 Build Docker image
    #   run: docker build -t project1-fastapi .


